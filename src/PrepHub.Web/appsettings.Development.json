{
  "//Logging": [
    "Ver o SQL que o EF gera é justamente o que se quer em desenvolvimento — em produção o",
    "appsettings.json silencia isso, senão o coletor de métricas encheria o log sozinho."
  ],
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Information"
    }
  },

  "//Observabilidade": [
    "Em desenvolvimento /metrics responde na porta da aplicação (PortaDeMetricas = 0): o `dotnet",
    "run` abre só as portas do launchSettings, e não existe rede interna de container para isolar.",
    "Com a app rodando, http://localhost:5090/metrics mostra a saída crua que o Prometheus lê.",
    "IntervaloDeColeta menor porque em desenvolvimento se cria uma conta e se quer vê-la no número."
  ],
  "Observabilidade": {
    "PortaDeMetricas": 0,
    "IntervaloDeColetaSegundos": 5
  }
}
